<?php

/**
 * Конфигурация для скрипта выборки данных из Яндекс Метрики.
 * Заполните все обязательные параметры перед запуском ym-fetch.php.
 */

// -----------------------------------------------------------------------
// Обязательные параметры
// -----------------------------------------------------------------------

// OAuth-токен для доступа к API Яндекс Метрики.
// Получить: https://oauth.yandex.ru/ (приложение с правами metrika:read)
define('YM_TOKEN', 'YOUR_OAUTH_TOKEN_HERE');

// Номер счётчика Яндекс Метрики (только цифры, например 12345678).
define('YM_COUNTER_ID', 'YOUR_COUNTER_ID_HERE');

// -----------------------------------------------------------------------
// Диапазон дат выборки
// -----------------------------------------------------------------------

// Дата начала периода в формате YYYY-MM-DD.
define('YM_DATE_FROM', date('Y-m-d', strtotime('-30 days')));

// Дата окончания периода в формате YYYY-MM-DD (включительно).
define('YM_DATE_TO', date('Y-m-d'));

// -----------------------------------------------------------------------
// Цели (Goals)
// -----------------------------------------------------------------------

// Список ID целей, по которым нужно считать конверсии и достижения.
// Например: [10, 25, 42]
// Если целей нет — оставьте пустой массив [].
define('YM_GOAL_IDS', []);

// -----------------------------------------------------------------------
// Директория для CSV-файлов
// -----------------------------------------------------------------------

// Абсолютный или относительный путь к папке, куда будут сохранены CSV.
// Папка создаётся автоматически, если не существует.
define('YM_OUTPUT_DIR', __DIR__ . '/ym-data');

// -----------------------------------------------------------------------
// Технические параметры API
// -----------------------------------------------------------------------

// Максимальное количество строк в одном запросе к API (лимит Яндекса — 100000).
define('YM_API_LIMIT', 10000);

// Таймаут HTTP-запроса к API в секундах.
define('YM_HTTP_TIMEOUT', 30);
